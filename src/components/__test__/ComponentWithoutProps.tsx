// components/ComponentWithoutProps.tsx

function ComponentWithoutProps() {
  return (
    <div>
      <h1>This is a component without props</h1>
      <p>It doesn't accept any data from its parent.</p>
    </div>
  );
}

export default ComponentWithoutProps;
